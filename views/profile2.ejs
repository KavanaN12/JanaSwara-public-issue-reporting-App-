<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attractive Profile Page</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #6a11cb, #2575fc);
            color: #fff;
        }
        nav {
            margin-top: 0.5rem;
        }

        nav a {
            color: #fff;
            text-decoration: none;
            margin: 0 1rem;
            font-weight: bold;
        }

        nav a:hover {
            font-size: larger;
        }
        .menu-bar {
            background: #333;
            padding: 10px 0;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }
        .menu-bar ul {
            list-style: none;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
        }
        .menu-bar ul li {
            margin: 0 15px;
        }
        .menu-bar ul li a {
            text-decoration: none;
            color: #fff;
            font-size: 16px;
            font-weight: bold;
            transition: color 0.3s;
        }
        .menu-bar ul li a:hover {
            color: #6a11cb;
        }
        .profile-container {
            max-width: 700px;
            margin: 50px auto;
            background: #fff;
            color: #333;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }
        .profile-header {
            text-align: center;
            margin-bottom: 20px;
        }
        .profile-header h2 {
            margin: 0;
            font-size: 28px;
            color: #333;
        }
        .profile-photo {
            text-align: center;
            margin-bottom: 20px;
        }
        .profile-photo img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #6a11cb;
        }
        .profile-field {
            margin-bottom: 15px;
            padding: 10px 0;
            border-bottom: 1px solid #ddd;
        }
        .profile-field label {
            font-weight: bold;
            display: block;
            margin-bottom: 5px;
            color: #6a11cb;
        }
        .profile-field span {
            display: block;
            font-size: 16px;
        }
        .edit-mode input, .edit-mode textarea, .edit-mode select {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ddd;
            border-radius: 6px;
            font-size: 16px;
        }
        .button-container {
            text-align: center;
        }
        button {
            padding: 12px 25px;
            font-size: 16px;
            background: linear-gradient(135deg, #6a11cb, #2575fc);
            color: #fff;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            transition: background 0.3s ease;
        }
        button:hover {
            background: linear-gradient(135deg, #2575fc, #6a11cb);
        }

        #editButton a, #logoutButton a{
            text-decoration: none;
            color: white;
        }

    </style>
</head>
<body>

    <div class="profile-container">
        <form id="profileForm">
            <div class="profile-header">
                <div class="profile-photo">
                    <!-- Display Profile Photo -->
                    <img src="/uploads/<%= user.photo %>" alt="Profile Photo" id="photo" style="max-width: 200px;">
                </div>
                <h1><%= user.name %>!</h1>
            </div>
            <div class="profile-field">
                <label for="name">Name:</label>
                <span id="nameDisplay"><%= user.name %></span>
            </div>
            <div class="profile-field">
                <label for="gender">Gender:</label>
                <span id="genderDisplay"><%= user.gender %></span>
            </div>
            <div class="profile-field">
                <label for="address">Address:</label>
                <span id="addressDisplay"><%= user.address %></span>
            </div>
            <div class="profile-field">
                <label for="email">Email:</label>
                <span id="emailDisplay"><%= user.email %></span>
            </div>
            <div class="profile-field">
                <label for="phone">Phone Number:</label>
                <span id="phoneDisplay"><%= user.phone %></span>
            </div>
            <div class="profile-field">
                <label for="sector">Sector:</label>
                <span id="sectorDisplay"><%= user.sector %></span>
            </div>
            <div class="profile-field">
                <label for="department">Department:</label>
                <span id="departmentDisplay"><%= user.department %></span>
            </div>
            <div class="profile-field">
                <label for="designation">Designation:</label>
                <span id="designationDisplay"><%= user.designation %></span>
            </div>
            <div class="profile-field">
                <label for="idProof">ID Proof Photo:</label>
                <img src="/uploads/<%= user.idProof %>" alt="ID Proof Photo" style="max-width: 200px;">
            </div>
            <div class="button-container">
                <button type="button" id="editButton"><a href="/edit-profile2/<%= user._id %>">Edit Profile</a></button>
                <button type="button" id="logoutButton"><a href="/logout">Logout</a></button>
            </div>
        </form>
    </div>

</body>
</html>